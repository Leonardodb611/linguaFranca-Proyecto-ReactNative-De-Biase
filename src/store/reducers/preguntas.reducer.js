import {answers} from '../../data/answers'

const INITIAL_STATE = {
    item: answers, 
    total: 200
}

const PreguntasReducer = (state = INITIAL_STATE, action) => {
    return state
}

export default PreguntasReducer